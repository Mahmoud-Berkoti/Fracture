version: "3.9"

services:
  target:
    build: ./target
    container_name: fracture-target
    # Bind to loopback only - BrokenCheckout is deliberately vulnerable
    # and MUST NOT be exposed to a public network. Change at your own risk.
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./target/data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  scanner:
    build: ./scanner
    container_name: fracture-scanner
    depends_on:
      target:
        condition: service_healthy
    volumes:
      - ./output:/app/output
    environment:
      - TARGET=brokencheckout
    command: python main.py --target brokencheckout --output /app/output
